/target

# Claude Code — only keep project-level settings and notify rule
.claude/*
!.claude/settings.local.json
!.claude/rules/
.claude/rules/*
!.claude/rules/notify.md

# Netlify CLI local state (machine-specific)
site/.netlify/
