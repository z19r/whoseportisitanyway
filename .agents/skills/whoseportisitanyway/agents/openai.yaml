interface:
  display_name: "Whoseportisitanyway"
  short_description: "Repo-specific patterns and workflows for whoseportisitanyway"
  default_prompt: "Use the whoseportisitanyway repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true